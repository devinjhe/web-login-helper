# Feature Backlog

Items to build and iterate on later.

- [x] Show notes field and created/updated dates in the login list UI
- [x] Allow editing existing login entries through the UI (not just add/delete)
- [x] Confirm before deleting — show a confirmation popup/dialog before removing an entry
- [ ] Improve auto-detection UX — when a login page is detected, open the popup automatically rather than requiring the user to click the extension icon
- [ ] Automated verification — add automated tests to verify storage layer, login page detection heuristics, and popup state logic
